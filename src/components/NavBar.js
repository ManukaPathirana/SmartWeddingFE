import React from 'react';
import { Link } from 'react-router-dom';

const NavBar = () => (
  <nav>
    <ul style={{ display: 'flex', gap: '1rem', listStyle: 'none', padding: 0 }}>
      {/* <li>
        <Link to="/dashboard">Dashboard</Link>
      </li> */}
      {/* Sample Invite removed */}
    </ul>
  </nav>
);

export default NavBar;
